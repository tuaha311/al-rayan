<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- windowBackground doubles as the launch screen: the logo is on screen
         from the first frame, before the WebView has anything to show. -->
    <style name="AppTheme" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/launch_screen</item>
        <item name="android:colorPrimary">@color/emerald</item>
        <item name="android:colorAccent">@color/emerald</item>
        <item name="android:statusBarColor">@color/bg</item>
        <item name="android:navigationBarColor">@color/bg</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
