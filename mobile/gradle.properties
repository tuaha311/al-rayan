org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
android.useAndroidX=false
android.nonTransitiveRClass=true

# The storefront the app opens. Point this at the production host before
# cutting a release — it is compiled into the APK as BuildConfig.BASE_URL.
alrayan.baseUrl=https://alrayancloths.com
